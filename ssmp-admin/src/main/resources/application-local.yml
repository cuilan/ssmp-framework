server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/zy_test?useUnicode=true&characterEncoding=UTF-8&useSSL=false&serverTimezone=UTC&autoReconnect=true
    username: root
    password: 123456
  redis:
    host: 127.0.0.1
    port: 6379

ssmp:
  # 是否开启权限验证
  enablePermission: false
  sysUser:
    # 系统用户登录次数限制
    loginTimes: 100